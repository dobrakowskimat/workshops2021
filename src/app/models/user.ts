export interface User {
  firstName: string;
  lastName: string;
  id: number;
  email: string;
}

export interface UserView {
  firstName: string;
  lastName: string;
  id: number;
  email: string;
}
