import { MyIfElseDirective } from './my-if-else.directive';

describe('MyIfElseDirective', () => {
  it('should create an instance', () => {
    const directive = new MyIfElseDirective();
    expect(directive).toBeTruthy();
  });
});
