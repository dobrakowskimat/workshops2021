import { AddBorderOnClickDirective } from './add-border-on-click.directive';

describe('AddBorderOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new AddBorderOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
